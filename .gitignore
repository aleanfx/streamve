# Bundle de Claude Design (referencia local, no va al repo)
_handoff/
*.zip

# Prompts de trabajo en la raíz (la copia versionada vive en docs/)
prompt-*.md

# Sistema
Thumbs.db
desktop.ini
.DS_Store

# Dependencias / build (para cuando haya backend)
node_modules/
.vercel
.env
.env.local

.vercel

# Originales pesados de los renders (quedan locales, se versiona el optimizado)
assets/logos/logo*.jpeg
assets/logos/Crear_logo_*.png
assets/cards/*.jpeg
assets/cards/*.jpg
assets/cards/*.png

# Video fuente de Flow (queda local; se versiona hero.mp4 optimizado)
assets/Woman_*.mp4
